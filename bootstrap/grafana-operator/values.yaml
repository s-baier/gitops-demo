grafana-operator:
  grafana:
    ingress:
      enabled: true
      hostname: grafana.demo.local
      ingressClassName: contour
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-staging
      tls: true
      # tlsSecret: grafana.demo.cn.evoila-lab.work-tls
